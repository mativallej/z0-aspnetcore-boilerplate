version: '3.8'

# Development overrides
services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    volumes:
      - ../logs:/app/logs
    ports:
      - "5000:8080"
